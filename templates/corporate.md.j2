# Security Report – Nmap Scan

## Executive Summary

- Total hosts analyzed: {{ data.stats.total if data.stats.total is defined else data.hosts | length }}
- Active (reachable) hosts: {{ data.stats.up if data.stats.up is defined else data.hosts | length }}
{% if data.total_vulns and data.total_vulns > 0 -%}
- Total vulnerabilities identified: {{ data.total_vulns }}
- Severity distribution:
  {% for sev, count in data.severity_counts.items() -%}
  **{{ sev }}**: {{ count }} {% if not loop.last %}|{% endif %}
  {% endfor -%}
{% else -%}
- No known vulnerabilities were identified on the scanned hosts.
{% endif %}


## Detailed Findings

{% for host in data.hosts -%}
### Host: {{ host.ip }}{% if host.hostnames %} ({{ host.hostnames | join(', ') }}){% endif %}

{% if host.ports and host.ports | length > 0 -%}
{% for port in host.ports -%}
**Port {{ port.port }}/{{ port.protocol }}** – {{ port.service }}{% if port.product %} ({{ port.product }} {{ port.version }}){% endif %}  
{% if port.vulns and port.vulns | length > 0 -%}
{% for vuln in port.vulns -%}
- **{{ vuln.cve }}** – CVSS: {{ "%.1f"|format(vuln.cvss) if vuln.cvss is not none else "N/A" }} ({{ vuln.severity or "Unknown" }}){% if vuln.cwe %} – CWE: {{ vuln.cwe }}{% endif %}  
  **Recommendation:** {{ vuln.recommendation or "N/A" }}
{% endfor -%}
{% else -%}
- No known vulnerabilities detected for this service.
{% endif -%}

{% endfor -%}
{% else -%}
*(No open ports detected on this host.)*

{% endif -%}
{% endfor -%}
